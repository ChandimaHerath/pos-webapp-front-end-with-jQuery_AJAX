{
  "name": "pos-web-app-frontend",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf ./docs",
    "build": "parcel build index.html 404.html manage-customers.html manage-items.html place-order.html search-orders.html --out-dir ./docs",
    "dev": "parcel serve index.html --open",
    "start": "npm run build && npm run dev"
  },
  "devDependencies": {
    "@types/jquery": "^3.5.6",
    "cssnano": "^5.0.8",
    "parcel-bundler": "^1.12.4",
    "postcss": "^8.3.9"
  },
  "dependencies": {
    "admin-lte": "^3.1.0",
    "jquery": "^3.6.0"
  }
}
